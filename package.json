{
  "name": "lovely-alert-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "LovelyAlert — a beautiful, modern, accessible, themeable alert / modal / toast library + MCP server.",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=18.20.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter lovely-alert dev",
    "check:dist": "node scripts/check-dist.mjs",
    "e2e": "playwright test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-version.mjs && pnpm install --lockfile-only",
    "release": "pnpm build && changeset publish",
    "clean": "pnpm -r exec rimraf dist .astro && rimraf node_modules"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.27.11",
    "@playwright/test": "^1.49.1",
    "typescript": "5.6.3"
  }
}
