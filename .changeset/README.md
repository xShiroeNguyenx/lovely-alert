# Changesets

This folder is used by [Changesets](https://github.com/changesets/changesets) to manage
versioning and changelogs for the publishable packages. Versioning/publishing happens in
**Phase 12** (see PLAN.md). The `@changesets/cli` dev dependency is added then.

To record a change once the CLI is installed: `pnpm changeset`.
